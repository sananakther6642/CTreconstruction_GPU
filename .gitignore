# Build artifacts
build/
*.o
*.a
__pycache__/
*.pyc

# torch JIT extension build cache
.cache/torch_extensions/

# Output data
*.hdf5
*.h5

# Archived results (kept locally only, not pushed -- too large for the repo)
submission_outputs/

# macOS
.DS_Store

# Presentations
*.pptx

# LaTeX build artifacts only (not source/output)
*.log
*.aux
*.toc
*.out
*.fls
*.fdb_latexmk
*.synctex.gz

# IDE
.vscode/
.idea/
*.code-workspace
.code-review-graph/

# Session logs (local only, never pushed)
sessions/

# Investigation docs (local only, never pushed)
docs/

# Build artifacts
build/
*.o
*.a

# Output data
*.hdf5
*.h5
# Except the archived submission results (tracked via Git LFS)
!submission_outputs/**/*.hdf5

# macOS
.DS_Store

# Presentations
*.pptx

# LaTeX build artifacts only (not source/output)
*.log
*.aux
*.toc
*.out
*.fls
*.fdb_latexmk
*.synctex.gz

# IDE
.vscode/
.idea/
*.code-workspace
.code-review-graph/

# Session logs (local only, never pushed)
sessions/

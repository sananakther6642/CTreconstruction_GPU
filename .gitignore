# Build artifacts
build/
*.o
*.a
__pycache__/
*.pyc

# torch JIT extension build cache
.cache/torch_extensions/

# Output data
*.hdf5
*.h5

# Archived results: figures/logs/hardware.txt are tracked (see results/),
# but *.hdf5 above already excludes the multi-GB reconstruction volumes.

# macOS
.DS_Store

# Presentations
*.pptx

# LaTeX build artifacts (the report itself lives under docs/, already
# ignored below; these patterns catch stray builds elsewhere)
*.aux
*.toc
*.out
*.fls
*.fdb_latexmk
*.synctex.gz

# IDE
.vscode/
.idea/
*.code-workspace
.code-review-graph/

# Session logs (local only, never pushed)
sessions/

# Investigation docs (local only, never pushed)
docs/

# Correspondence drafts and ad-hoc diagnostic dumps at repo root
# (local only, not part of the submission)
email_guangpu_yang*.txt
diag_bp_ones_output.txt
log_div.txt
out.txt
